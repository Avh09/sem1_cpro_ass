 // int freq[26];
    // for (int i = 0; i<26; i++)
    //     freq[i] = 0;

    // for (int i = 0; i<strlen(s); i++)
    //     freq[s[i] - 'a']++;

    // // find max in freq array and print

    // int max = 0;
    // for (int i = 0; i<26; i++)
    // {
    //     if (freq[i]>max)
    //         max = freq[i];
    // }